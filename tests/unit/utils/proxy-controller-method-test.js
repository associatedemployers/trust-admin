import proxyControllerMethod from 'trust-admin/utils/proxy-controller-method';

module('proxyControllerMethod');

// Replace this with your real tests.
test('it works', function() {
  var result = proxyControllerMethod();
  ok(result);
});
