import {
  parseChangeCode
} from 'trust-admin/helpers/parse-change-code';

module('ParseChangeCodeHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = parseChangeCode(42);
  ok(result);
});
