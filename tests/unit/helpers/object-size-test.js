import {
  objectSize
} from 'trust-admin/helpers/object-size';

module('ObjectSizeHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = objectSize(42);
  ok(result);
});
