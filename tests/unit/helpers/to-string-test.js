import {
  toString
} from 'trust-admin/helpers/to-string';

module('ToStringHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = toString(42);
  ok(result);
});
