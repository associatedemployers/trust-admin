import {
  concatArray
} from 'trust-admin/helpers/concat-array';

module('ConcatArrayHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = concatArray(42);
  ok(result);
});
