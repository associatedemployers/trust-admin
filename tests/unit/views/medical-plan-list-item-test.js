import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:medical-plan-list-item', 'MedicalPlanListItemView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
